---
interface Props {
  class?: string;
  delay?: 1 | 2 | 3 | 4 | 5;
}

const { class: className = "", delay = 1 } = Astro.props;
const delayClass = delay > 1 ? "reveal-delay-" + delay : "";
---

<div class:list={["reveal", delayClass, className]}>
  <slot />
</div>
