---
import Layout from "../../core/layout/Layout.astro";
import { projects } from "../../features/portfolio/projects";
import MobileHero from "../../features/portfolio/mobile/MobileHero.astro";
import MobileFeatures from "../../features/portfolio/mobile/MobileFeatures.astro";
import MobileGallery from "../../features/portfolio/mobile/MobileGallery.astro";
import MobileTechStack from "../../features/portfolio/mobile/MobileTechStack.astro";

export function getStaticPaths() {
  const mobileProjects = projects.filter((p) => p.category === "mobile" && p.mobileDetails);
  return mobileProjects.map((project) => {
    return {
      params: { id: project.i18nKey },
      props: { project },
    };
  });
}

const { project } = Astro.props;
const { title, mobileDetails } = project;
---

<Layout pageTitle={`${title} - David Solorza`}>
  <!-- Custom Page Background -->
  <div class="fixed inset-0 -z-50 bg-gradient-to-br from-gray-50 via-green-50/50 to-blue-50/50 dark:from-[#0a0a0a] dark:via-[#051014] dark:to-[#020813] pointer-events-none transition-colors duration-500"></div>

  <div class="pt-24 pb-16 min-h-screen relative z-0">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <!-- Back button -->
      <a href="/#projects" class="inline-flex items-center gap-2 text-gray-500 hover:text-blacktext dark:text-gray-400 dark:hover:text-white transition-colors mb-12">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5"><path d="m15 18-6-6 6-6"/></svg>
        <span class="font-medium" data-i18n="projects.goBack">Volver</span>
      </a>

      <!-- Hero Section -->
      <MobileHero project={project} />

      <!-- Features Section -->
      <div class="mt-32">
        <MobileFeatures project={project} />
      </div>

      <!-- Gallery Section -->
      <div class="mt-32">
        <MobileGallery project={project} />
      </div>

      <!-- Tech Stack -->
      <div class="mt-32 mb-16">
        <MobileTechStack project={project} />
      </div>

    </div>
  </div>
</Layout>
